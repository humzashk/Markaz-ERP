<%- include('../partials/header') %>
<div class="page-header">
  <h2><i class="bi bi-arrow-left-right"></i> Stock Movement Tracking</h2>
  <a href="/stock" class="btn btn-outline-secondary"><i class="bi bi-arrow-left"></i> Back to Adjustments</a>
</div>

<div class="card mb-3">
  <div class="card-body py-2">
    <form method="GET" class="row g-2 align-items-end">
      <div class="col-md-6">
        <input type="text" name="search" class="form-control form-control-sm" placeholder="Search product..." value="<%= search %>">
      </div>
      <div class="col-md-3">
        <select name="type" class="form-select form-select-sm">
          <option value="">All Movements</option>
          <option value="outbound" <%= type==='outbound'?'selected':'' %>>Outbound (Orders)</option>
          <option value="inbound" <%= type==='inbound'?'selected':'' %>>Inbound (Invoices)</option>
        </select>
      </div>
      <div class="col-md-3">
        <button class="btn btn-sm btn-secondary w-100">Filter</button>
      </div>
    </form>
  </div>
</div>

<div class="card">
  <div class="card-body p-0">
    <div class="table-responsive">
      <table class="table table-hover mb-0">
        <thead class="table-light">
          <tr>
            <th>Date</th>
            <th>Product</th>
            <th>Party</th>
            <th>Type</th>
            <th class="text-end">Qty (Ctn)</th>
            <th>Reference</th>
          </tr>
        </thead>
        <tbody>
          <% movements.forEach(m => { %>
          <tr>
            <td><small><%= formatDate(m.movement_date) %></small></td>
            <td>
              <strong><a href="/products/view/<%= m.product_id %>" class="text-decoration-none"><%= m.product_name %></a></strong>
            </td>
            <td><%= m.party_name %></td>
            <td>
              <% if (m.party_type === 'Customer') { %>
                <span class="badge bg-danger">Outbound</span>
              <% } else { %>
                <span class="badge bg-success">Inbound</span>
              <% } %>
            </td>
            <td class="text-end fw-bold"><%= Math.floor((Number(m.quantity)||0) / Math.max(1, m.qty_per_pack||1)) %> Ctn</td>
            <td>
              <% if (m.doc_type === 'Invoice' && m.invoice_id) { %>
                <a href="/invoices/view/<%= m.invoice_id %>" class="btn btn-sm btn-outline-primary">
                  <i class="bi bi-eye"></i> <%= m.reference_number %>
                </a>
              <% } else if (m.doc_type === 'Order' && m.order_id) { %>
                <a href="/orders/view/<%= m.order_id %>" class="btn btn-sm btn-outline-info">
                  <i class="bi bi-eye"></i> <%= m.reference_number %>
                </a>
              <% } else if (m.doc_type === 'Purchase' && m.purchase_id) { %>
                <a href="/purchases/view/<%= m.purchase_id %>" class="btn btn-sm btn-outline-info">
                  <i class="bi bi-eye"></i> <%= m.reference_number %>
                </a>
              <% } else { %>
                <span class="text-muted small"><%= m.doc_type %> · <%= m.reference_number %></span>
              <% } %>
            </td>
          </tr>
          <% }) %>
          <% if (!movements.length) { %>
            <tr>
              <td colspan="6" class="text-center text-muted py-4">
                <% if (search || type) { %>
                  No movements found matching your criteria
                <% } else { %>
                  No stock movements found
                <% } %>
              </td>
            </tr>
          <% } %>
        </tbody>
      </table>
    </div>
  </div>
</div>

<div class="mt-3 text-muted small">
  <p><strong>Legend:</strong></p>
  <ul class="mb-0">
    <li><span class="badge bg-danger">Outbound</span> — Stock leaving (sales to customers, transfers out, damage)</li>
    <li><span class="badge bg-success">Inbound</span> — Stock entering (purchases from vendors, returns, transfers in)</li>
  </ul>
</div>

<%- include('../partials/footer') %>
